apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: jubalzwolle
spec:
  template:
    spec:
      containers:
        - name: web
          image: jubalzwolle
          envFrom:
            - secretRef:
                name: jubalzwolle-env