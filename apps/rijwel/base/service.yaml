apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: rijwel
spec:
  template:
    spec:
      containers:
        - name: web
          image: rijwel
          envFrom:
            - secretRef:
                name: rijwel-env